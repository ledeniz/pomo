import React from 'react'

const Keybindings = ({  }) => {
  return (
    <div>
      <span className="link-title">keybindings</span>
      <div className="keybindings">
        <div>start / pause — <kbd>spacebar</kbd></div>
        <div>reset — <kbd>r</kbd></div>
      </div>
    </div>
  )
}

export default Keybindings
